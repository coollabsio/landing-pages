{
    "name": "coollabs-landingpage",
    "type": "module",
    "scripts": {
        "dev": "NODE_ENV=development vite dev",
        "build": "NODE_ENV=production vite build",
        "preview": "svelte-kit preview",
        "start": "node build",
        "lint": "prettier --check . && eslint --ignore-path .gitignore .",
        "format": "prettier --write ."
    }
}